# Optional local image repository for Docker Compose.
IMAGE_REPO=local
# Optional image tag for Docker Compose.
IMAGE_TAG=dev
# Host port for frontend.
FRONTEND_PORT=3000
# Memory limit for frontend container.
FRONTEND_MEM_LIMIT=768m
